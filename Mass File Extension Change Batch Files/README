These batch files will change the extensions of all files in a folder.

To use, place the corresponding batch file and all .rom files that you want to change in a folder, then double click the batch file.
